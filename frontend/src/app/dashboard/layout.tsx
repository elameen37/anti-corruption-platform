import SovereignAppShell from "@/components/SovereignAppShell";

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
    return <SovereignAppShell>{children}</SovereignAppShell>;
}
